# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*~
.idea/
.vscode/

# Claude Code config and skills are tracked on purpose — the whole .claude/
# config (settings + skills) lives in git so it is reusable across machines.
